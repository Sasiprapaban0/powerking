import React from 'react'
import { Outer, Header } from '../theme/headerWrapper'

const HeaderFormComponent = () => (
    <Outer>
        <Header>
            
            <h2>Welcome to React</h2>
        </Header>
    </Outer>
)
export default HeaderFormComponent